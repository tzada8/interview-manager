<%= form_with(model: my_question) do |form| %>
  <% if my_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(my_question.errors.count, "error") %> prohibited this my_question from being saved:</h2>

      <ul>
        <% my_question.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :prompt %>
    <%= form.text_field :prompt %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
